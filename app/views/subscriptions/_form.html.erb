<%= form_with(model: subscription, local: true) do |form| %>
  <% if subscription.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subscription.errors.count, "error") %> prohibited this subscription from being saved:</h2>

      <ul>
      <% subscription.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :voter_id %>
    <%= form.text_field :voter_id, id: :subscription_voter_id %>
  </div>

  <div class="field">
    <%= form.label :votation_id %>
    <%= form.text_field :votation_id, id: :subscription_votation_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
